export default function Footer() {
    return (
      <footer className="w-full py-6 mt-12 bg-gray-100 text-sm text-center text-gray-500">
        © {new Date().getFullYear()} KidMatch. Helping parents find the perfect gift, one personality at a time.
      </footer>
    );
  }